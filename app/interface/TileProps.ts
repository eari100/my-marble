import {PlayerInterface} from "@/app/interface/PlayerInterface";
import {TileInterface} from "@/app/interface/TileInterface";

export interface TileProps {
    tileData: TileInterface
    players: PlayerInterface[]
}
